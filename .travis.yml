language: java

# optionally specify a list of environments, for example to test different RDBMS
env:
  - DB=mysql

services:
  - mysql

before_script:
  - sh -c "if [ '$DB' = 'mysql' ]; then mysql -e 'create database IF NOT EXISTS books_manager; uroot fi"